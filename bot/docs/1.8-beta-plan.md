# 1.8 beta implementation and verification

Keep the Tauri/Python architecture and the Classic/Aero identity. Preserve existing settings. Work solo on a beta branch; stable users must not receive an experimental release automatically.

1. Audit input, recovery, local HTTP/IPC, settings and release boundaries; fix evidenced issues and document remaining risks.
2. Add a cancellable respawn settling delay before the existing bounded W hold; release on Stop, focus loss and errors.
3. Add persistent left/right/Windows mouse-button selection covering every automated selection route.
4. Add opt-in Health/Ki restart-after-hit with a bounded menu handshake, fresh detector state, and interruption/completion checks. Keep off by default until gameplay acceptance.
5. Expose a clearly scoped main-loop scan limit and measured loop rate; preserve fast inner minigame tracking. Distinguish measured throughput from successful game hits.
6. Move training order to the top, group WASD mode/timing, repair small-window queue and native-looking resize button, clarify timing labels and mode applicability, and extend settings search. Preserve the overall look.
7. Deliver a portable style guide and token stylesheet. Assess incremental TypeScript without making a full migration a release dependency.
8. Run Python/frontend/Rust checks, security dependency audits where available, and browser-rendered layout checks with a mock backend. Verify packaged sidecar via CI. Publish a prerelease only after the applicable checks pass; keep stable updater feed isolated.

Live acceptance still requires actual DBOG sessions: slow respawns, the friend's remapper, earned progress after restart, and slow-PC timing. No claim of those outcomes from unit tests or mocked screenshots.
